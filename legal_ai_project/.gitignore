# Environment
.env
.env.*

# Django
db.sqlite3
*.log

# Python cache
__pycache__/
*.pyc
*.pyo

# Media uploads (user-uploaded files — stored on Render persistent disk)
media/

# Static collected files (generated by collectstatic)
staticfiles/

# Virtual environment
venv/
env/

# NOTE: pytorch_model.bin is tracked via Git LFS — do NOT ignore it
# NOTE: data/ artifacts (embeddings, model.pkl) are committed directly
